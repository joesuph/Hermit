dad := sam + 3

A _x := dad + _x

#this works
D lamb := (A 4) + 3

# Also works
B cat := A 4 + 3

#stored as expected
bad_rule := _a is bad -> don't elect _a
fact := john is bad

oth := bad_rule(fact)

#peano arithmetic
zero := 0 : num

succ := (_a : num) -> ((next _a) : num)

text_of_num := (_a) -> ( print(_a) ) 

succ(zero)

py(text_of_num(4))




#how to mix symbols and functions? Can rewrite rules state facts about functions?







#Gather entailments for a certain context
