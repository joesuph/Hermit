dad := sam + 3

A _x := dad + _x

#this works
D lamb := (A 4) + 3

# Also works
B cat := A 4 + 3

#stored as expected
bad_rule := _a is bad -> don't elect _a
fact := john is bad


#bad example
oth := bad_rule(fact)

#imply zero
zero := 0 : num

succ := (_a : num) -> ((next _a) : num)

succ(succ(zero))

#Gather entailments for a certain context
